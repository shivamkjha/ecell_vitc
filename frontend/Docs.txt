CREATED USING VITE
 
{ 
    CHANGES:
    1. main.jsx instead of index.jsx
    2. npm run dev instead of npm start

    STRUCTURE:
    app.jsx -> Homepage.jsx -> other components 
}


react-scroll :  Library : to scroll to different positions on page.

overflow-x-auto : horizontal scrolling (in Blogs)

routing : outlet works as a placeholder to substitute different routes at its place (ref app.jsx)


//! mx-auto 
//! my-auto 
//! px-auto